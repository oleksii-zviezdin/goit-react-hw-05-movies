import { Rings } from 'react-loader-spinner'
import { LoaderWrapper } from './Loader.styled';

const Loader = () => {
    return (
    <LoaderWrapper>
        <Rings/>
    </LoaderWrapper>
);
};

export default Loader